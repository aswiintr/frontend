// Button.tsx

import { useState } from "react";

import ./styles.css"; import icon from "./reload.svg";

export const Button = () => { const [isLoading, setIsLoading]

useState<boolean>(false);

const handleClick = async () => {

setIsLoading(true);

// await api call setIsLoading(false);

};

return (

<button disabled=fisLoading}

onClick={handleClick)

className="button ${tsLoading

? "loading":""}}

<img src={iconSettings} /> <span>fisLoading

? "Reloading": "Reload App"} </span>

</button>

=

); };
